int main () {
    double i = 2.5;
    // printInt(i);
    // int j = 5;
    // printInt(i*j);

    if (i >= 1.5) printInt(7); else printInt(10);
    // if (true) int i = 1; else int f = 0;
    // int a,b,c;
    // int x = 3;
    // printInt(x);
    // while (false) int x = 0;
    // printInt(x);
    return 0;
}